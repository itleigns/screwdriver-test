shared:
  image: node:18

jobs:
  test:
    requires: [ ~pr, ~commit ]
    steps:
      - install: echo b
  test-2:
    template: screwdriver-cd-test/test-trusted-template@latest
    requires: [ ~pr, ~commit ]
  test-3:
    requires: [ ~pr, ~commit ]
    steps:
      - install: echo a
